package utils

const (
	//AppVersion is set to the current version of the app
	AppVersion = "0.1"
	//AppChannel is set to the current dev channel of the app
	AppChannel = "Dev"
	//WorkerVersion is set to the current version of the worker
	WorkerVersion = "0.1"
	//WorkerChannel is set to the current dev channel of the worker
	WorkerChannel = "Dev"
)
